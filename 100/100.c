#include<stdio.h>
#include<stdlib.h>

int main()
{
    int floor=0,ceil=0,max=0,count=0; 

    while(scanf("%d %d", &floor, &ceil)!=EOF)
    {
        max=0;
        printf("%d %d ", floor, ceil);
        for(int i=floor;i<=ceil;i++)
        {
            count=0;
            while()
        }
    }

    return 0;
}